library("usethis")
git_sitrep() # git situation report


## if you have no token set
## then create the token for the repository you want to work with on git hub remember to copy it from the page where it is created

# then you can set it using the in the git situation report.

# don't write down the token in the files you push as github will scan for them and revoke the token...duh!